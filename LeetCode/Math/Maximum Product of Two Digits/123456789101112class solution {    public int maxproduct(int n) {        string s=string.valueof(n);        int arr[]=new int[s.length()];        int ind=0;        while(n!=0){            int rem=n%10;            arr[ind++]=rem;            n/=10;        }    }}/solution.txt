class Solution {
    public int maxProduct(int n) {
        String s=String.valueOf(n);
        int arr[]=new int[s.length()];
        int ind=0;
        while(n!=0){
            int rem=n%10;
            arr[ind++]=rem;
            n/=10;
        }
    }
}
